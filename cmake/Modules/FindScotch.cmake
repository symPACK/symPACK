if (NOT (SCOTCH_INCLUDES AND SCOTCH_LIBRARIES))
  find_path(SCOTCH_INCLUDES NAMES scotch.h PATHS $ENV{SCOTCH_DIR} PATH_SUFFIXES include)
  find_library(SCOTCH_LIBRARY       scotch      PATHS $ENV{SCOTCH_DIR} PATH_SUFFIXES lib)
  find_library(PTSCOTCH_LIBRARY     ptscotch    PATHS $ENV{SCOTCH_DIR} PATH_SUFFIXES lib)
  find_library(SCOTCHERR_LIBRARY    scotcherr   PATHS $ENV{SCOTCH_DIR} PATH_SUFFIXES lib)
  find_library(PTSCOTCHERR_LIBRARY  ptscotcherr PATHS $ENV{SCOTCH_DIR} PATH_SUFFIXES lib)
  set(SCOTCH_LIBRARIES ${SCOTCH_LIBRARY} ${PTSCOTCH_LIBRARY} ${SCOTCHERR_LIBRARY} ${PTSCOTCHERR_LIBRARY})
endif()

include(FindPackageHandleStandardArgs)
find_package_handle_standard_args(SCOTCH DEFAULT_MSG SCOTCH_INCLUDES SCOTCH_LIBRARIES)
